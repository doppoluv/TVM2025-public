AddMul {
  AddExpr = AddExpr "+" MulExpr  -- add
          | MulExpr
  MulExpr = MulExpr "*" Primary  -- mul
          | Primary
  Primary = number
          | "(" AddExpr ")"  -- paren
  number = digit+
}